
<%= form_for @user, :as => :user, :url => session_path(@user) do |f| %>

  <%= render :partial => 'users_sessions/login_register_form', :locals => { :f => f}  %>

  <div>
    <%= f.submit 'Login' %>
  </div>

<% end %>
